# Postgres connection string
# Use a read-only user with only SELECT privileges
DATABASE_URL=postgresql://readonly_user:password@localhost:5432/dbname

# Optional: Set query timeout in milliseconds (default: 30000)
# QUERY_TIMEOUT=30000

# Optional: Set log level (error, warn, info, debug - default: info)
# LOG_LEVEL=info

